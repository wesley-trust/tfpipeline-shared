steps:

# Go Steps
- template: ../Tasks/go.yml
  parameters:
    arguments: '-v unit_test.go'

# Regula steps
- template: ../Tasks/regula.yml

# Regula scan step
- template: ../Tasks/regula_scan_hcl.yml